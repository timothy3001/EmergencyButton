const char *WIFI_SSID = "<wifi ssid>";
const char *WIFI_PASSWORD = "<wifi password>";

char *EMAIL_SUBJECT = "<subject of the email (something like 'EMERGENCY!')>";
char *EMAIL_SENDER = "<GMail address>";
char *EMAIL_PASSWORD_BASE64 = "<GMail password>";

char *EMAIL_RECEIVER = "<receipient email address";
char *EMAIL_BODY_PREFIX = "<Text in the email body before voltage is printed (recommended: 'Battery voltage: ')>";